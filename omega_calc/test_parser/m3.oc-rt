# Omega Calculator v1.2 (based on Omega Library 1.2, August, 2000):
# I := {[i,j] : 1 <= i+j,j <= 10};
# 
# codegen I;
for(t1 = -9; t1 <= 9; t1++) {
  for(t2 = max(-t1+1,1); t2 <= min(-t1+10,10); t2++) {
    s1(t1,t2);
  }
}

# 
